<Window
    x:Class="FufuLauncher.Views.DiagnosticsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:FufuLauncher.ViewModels"
    mc:Ignorable="d"
    Title="系统诊断">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Height="48" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="硬件与系统环境" 
                       Grid.Column="1"
                       VerticalAlignment="Center" 
                       Margin="24,0,0,0"
                       FontSize="14" 
                       FontWeight="SemiBold"/>
        </Grid>

        <Grid Grid.Row="1" Padding="24,0,24,24">
            
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10"
                        Visibility="{x:Bind ToVisible(ViewModel.IsLoading), Mode=OneWay}">
                <ProgressRing IsActive="True"/>
                <TextBlock Text="正在收集系统信息..." Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <ScrollViewer Visibility="{x:Bind ToCollapsed(ViewModel.IsLoading), Mode=OneWay}">
                <StackPanel Spacing="20">
                    <TextBlock Text="显示与图形" Style="{StaticResource SubtitleTextBlockStyle}"/>
      
                    <Grid ColumnSpacing="12" RowSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" Padding="12">
                            <StackPanel>
                                <TextBlock Text="分辨率" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12"/>
                                <TextBlock Text="{x:Bind ViewModel.Info.ScreenResolution, Mode=OneWay}" FontSize="16" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" Padding="12">
                            <StackPanel>
                                <TextBlock Text="刷新率" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12"/>
                                <TextBlock Text="{x:Bind ViewModel.Info.CurrentRefreshRate, Mode=OneWay}" FontSize="16" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <TextBlock Text="核心硬件" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,10,0,0"/>
                    <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                        <StackPanel Spacing="16">
                            <Grid>
                                <TextBlock Text="OS" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind ViewModel.Info.OsVersion, Mode=OneWay}" HorizontalAlignment="Right" MaxWidth="300" TextWrapping="Wrap"/>
                            </Grid>
                            <Border Height="1" Background="{ThemeResource SurfaceStrokeColorDefaultBrush}"/>
                            <Grid>
                                <TextBlock Text="CPU" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind ViewModel.Info.CpuName, Mode=OneWay}" HorizontalAlignment="Right" MaxWidth="300" TextTrimming="CharacterEllipsis"/>
                            </Grid>
                            <Border Height="1" Background="{ThemeResource SurfaceStrokeColorDefaultBrush}"/>
                            <Grid>
                                <TextBlock Text="GPU" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind ViewModel.Info.GpuName, Mode=OneWay}" HorizontalAlignment="Right" MaxWidth="300" 
                                           TextTrimming="CharacterEllipsis"/> </Grid>
                            <Border Height="1" Background="{ThemeResource SurfaceStrokeColorDefaultBrush}"/>
                            <Grid>
                                <TextBlock Text="RAM" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind ViewModel.Info.TotalMemory, Mode=OneWay}" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <InfoBar IsOpen="True" IsClosable="False" Severity="Informational" Title="建议" Message="{x:Bind ViewModel.Info.Suggestion, Mode=OneWay}"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        </Grid>
</Window>