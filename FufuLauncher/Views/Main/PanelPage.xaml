<Page
    x:Class="FufuLauncher.Views.PanelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:FufuLauncher.ViewModels"
    xmlns:models="using:FufuLauncher.Models"
    xmlns:converters="using:FufuLauncher.Converters"
    d:DataContext="{d:DesignInstance Type=viewmodels:ControlPanelModel}"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <converters:EmptyCountToVisibilityConverter x:Key="EmptyCountToVisibilityConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid>
        <NavigationView
            IsBackButtonVisible="Collapsed"
                        PaneDisplayMode="LeftMinimal"
                        IsPaneToggleButtonVisible="True"
                        IsSettingsVisible="False"
                        SelectionChanged="NavigationView_SelectionChanged"
                        Margin="0,40,0,0">
            
            <NavigationView.MenuItems>
                <NavigationViewItem Content="基础设置" Tag="BasicSettings" IsSelected="True">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE115;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem Content="高级设置" Tag="AdvancedSettings">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE115;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            
            <ScrollViewer x:Name="ContentScrollViewer" 
                          VerticalScrollBarVisibility="Auto"
                          Tapped="ContentScrollViewer_Tapped">
                
                <Grid Padding="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="350"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Grid Grid.Row="1">
                            <Grid x:Name="BasicSettingsPage" Visibility="Visible">
                                <StackPanel Spacing="20">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="游戏控制面板" 
                                                   FontSize="24" 
                                                   FontWeight="Bold" 
                                                   Margin="40,0,0,10"/>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,40,10">
                                            <TextBlock Text="注入" VerticalAlignment="Center" FontSize="14"/>
                                            <ToggleSwitch IsOn="{x:Bind MainViewModel.UseInjection, Mode=TwoWay}" />
                                        </StackPanel>
                                    </Grid>
                                    
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Row="0" Text="FPS 设置" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                                        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="20">
                                            <StackPanel Orientation="Vertical" Spacing="10">
                                                <ToggleSwitch Header="启用 FPS 突破" 
                                                              IsOn="{Binding EnableFpsOverride, Mode=TwoWay}" 
                                                              ToolTipService.ToolTip="解锁游戏帧率上限，允许超过默认的 60 FPS"/>
                                            </StackPanel>
    
                                            <StackPanel Orientation="Vertical" Spacing="5" Width="300" VerticalAlignment="Center">
                                                <Slider Header="目标 FPS" 
                                                        Value="{Binding TargetFps, Mode=TwoWay}" 
                                                        Minimum="30" Maximum="1000" StepFrequency="10" 
                                                        ToolTipService.ToolTip="拖动调整期望的最大帧率"/>
        
                                                <NumberBox Value="{Binding TargetFps, Mode=TwoWay}" 
                                                           Minimum="30" Maximum="1000" 
                                                           SpinButtonPlacementMode="Hidden" Width="100" HorizontalAlignment="Left" 
                                                           GotFocus="NumberBox_GotFocus" LostFocus="NumberBox_LostFocus" 
                                                           ToolTipService.ToolTip="输入目标帧率数值"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                    
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="FOV 设置" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                                        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="20">
                                            <StackPanel Orientation="Vertical" Spacing="10">
                                                <ToggleSwitch Header="启用 FOV 修改" IsOn="{Binding EnableFovOverride, Mode=TwoWay}" 
                                                              ToolTipService.ToolTip="允许自定义视野范围，获得更宽广的视角"/>
                                            </StackPanel>

                                            <StackPanel Orientation="Vertical" Spacing="5" Width="300" VerticalAlignment="Center">
                                                <Slider Header="目标 FOV" Value="{Binding TargetFov, Mode=TwoWay}" Minimum="30" Maximum="120" StepFrequency="1" />
                                                <NumberBox Value="{Binding TargetFov, Mode=TwoWay}" Minimum="30" Maximum="120" SpinButtonPlacementMode="Hidden" Width="100" HorizontalAlignment="Left" GotFocus="NumberBox_GotFocus" LostFocus="NumberBox_LostFocus" 
                                                           ToolTipService.ToolTip="调整视野角度，数值越大视野越宽"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                    
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="基础游戏功能" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="20">
                                                <ToggleSwitch Header="移除任务横幅" 
                                                              IsOn="{Binding RemoveQuestBanner, Mode=TwoWay}" 
                                                              ToolTipService.ToolTip="移除地图左上角的任务图"/>
                  
                                                <ToggleSwitch Header="移除伤害文本" 
                                                              IsOn="{Binding RemoveDamageText, Mode=TwoWay}" 
                                                              ToolTipService.ToolTip="攻击时不显示伤害数字，使画面更简洁"/>
                                            </StackPanel>
                                            <ToggleSwitch Grid.Column="1" Header="触屏操作模式" 
                                                          IsOn="{Binding EnableTouchScreenMode, Mode=TwoWay}" 
                                                          HorizontalAlignment="Left" Margin="0,0,0,0" 
                                                          ToolTipService.ToolTip="在PC客户端使用手机触屏UI"/>
                                        </Grid>
                                    </Grid>
                                </StackPanel>
                            </Grid>
                            
                            <Grid x:Name="AdvancedSettingsPage" Visibility="Collapsed">
                                <StackPanel Spacing="20">
                                    <TextBlock Text="高级设置" 
                                               FontSize="24" 
                                               FontWeight="Bold" 
                                               Margin="40,0,0,10"/>
                                    
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="画面效果" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <ToggleSwitch Header="启用雾效去除" 
                                                          IsOn="{Binding EnableFogOverride, Mode=TwoWay}" 
                                                          ToolTipService.ToolTip="移除远处的环境雾气，使远景更清晰"/>
                  
                                            <ToggleSwitch Grid.Column="1" Header="启用反虚化" 
                                                          IsOn="{Binding EnablePerspectiveOverride, Mode=TwoWay}" 
                                                          ToolTipService.ToolTip="移除角色或物体的虚化/透明效果"/>
                                        </Grid>
                                    </Grid>
                                    
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="高级功能控制" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <ToggleSwitch Grid.Column="0" Header="禁用大招动画" 
                                                          IsOn="{Binding DisableEventCameraMove, Mode=TwoWay}" 
                                                          ToolTipService.ToolTip="释放元素爆发时不再播放特写镜头，保持当前视角"/>
              
                                            <ToggleSwitch Grid.Column="1" Header="移除配队条" 
                                                          IsOn="{Binding RemoveTeamProgressLimit, Mode=TwoWay}" 
                                                          ToolTipService.ToolTip="移除2秒配队界面的加载动画"/>
              
                                            <ToggleSwitch Grid.Column="2" Header="重定向合成入口" 
                                                          IsOn="{Binding EnableRedirectCombineEntry, Mode=TwoWay}" 
                                                          ToolTipService.ToolTip="随时随地打开合成台"/>
                                        </Grid>
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Grid>
                    
                    <Border Grid.Column="1" 
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Margin="20,0,0,0"
                            Padding="20">
                        <ScrollViewer>
                            <StackPanel Spacing="15">
                                <TextBlock Text="本周游戏时长统计" 
                                           FontSize="18" 
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,5"/>
                                
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Row="0" Grid.Column="0" Spacing="5">
                                        <TextBlock Text="总时长" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="{Binding WeeklyStats.DisplayTotalTime}" FontSize="18" FontWeight="SemiBold"/>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Row="0" Grid.Column="1" Spacing="5">
                                        <TextBlock Text="平均每日" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="{Binding WeeklyStats.DisplayAverageTime}" FontSize="18" FontWeight="SemiBold"/>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Row="1" Grid.Column="0" Spacing="5" Margin="0,15,0,0">
                                        <TextBlock Text="游戏状态" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        
                                        <Grid>
                                            <TextBlock Text="未运行" FontSize="16" FontWeight="SemiBold" 
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                       Visibility="{Binding IsGameRunning, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=True}"/>
                                            <TextBlock Text="运行中" FontSize="16" FontWeight="SemiBold" 
                                                       Foreground="#10B981"
                                                       Visibility="{Binding IsGameRunning, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </Grid>
                                    </StackPanel>
                                </Grid>
                                
                                <Rectangle Height="1" 
                                           Fill="{ThemeResource CardStrokeColorDefaultBrush}" 
                                           Margin="0,5"/>
                                
                                <TextBlock Text="每日详情" FontWeight="SemiBold" FontSize="14" Margin="0,5,0,0"/>
                                <ItemsControl ItemsSource="{Binding WeeklyStats.DailyRecords}" Margin="0,5,0,0">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="models:GamePlayTimeRecord">
                                            <Grid Margin="0,6">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10">
                                                    <TextBlock Text="{Binding DayOfWeek}" FontSize="14" Width="40"/>
                                                    <TextBlock Text="{Binding DisplayDate}" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                </StackPanel>
                                                
                                                <TextBlock Grid.Column="1" Text="{Binding DisplayTime}" FontSize="14" FontWeight="SemiBold"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                
                                <TextBlock Text="本周暂无游戏记录" 
                                           FontSize="14" 
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           HorizontalAlignment="Center"
                                           Margin="0,20"
                                           Visibility="{Binding WeeklyStats.DailyRecords.Count, Converter={StaticResource EmptyCountToVisibilityConverter}}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </ScrollViewer>
        </NavigationView>
        
        <TextBlock Text="{Binding ConnectionStatus}" 
                   FontSize="14" 
                   VerticalAlignment="Top"
                   HorizontalAlignment="Left"
                   Margin="50,10,0,0"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </Grid>
</Page>